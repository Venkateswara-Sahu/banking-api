version: '3.9'

services:
  banking-api:
    build:
      context: .
    ports:
      - "5000:5000"
    container_name: banking_api
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
